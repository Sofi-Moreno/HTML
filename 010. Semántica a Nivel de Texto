    ♥ Semántica a Nivel de Texto
Las etiquetas HTML definen la estructura y el significado del contenido en una página web.
Los atributos proporcionan información adicional sobre cómo se debe mostrar o comportar un
elemento. Es importante utilizar las etiquetas semánticas adecuadas para mejorar la
accesibilidad y el SEO de tu sitio web.

    Etiquetas de Semántica a Nivel de Texto:

1. <a> (anchor, ancla): Crea un hipervínculo a otra página web, archivo, ubicación dentro de la misma
página, permite descargar archivos, enviar emails o llamar a direcciones especificadas
en la etiqueta.
    Atributos comunes:
    - href: especifica la URL de destino.
    - target: define dónde se abrirá el enlace (por ejemplo, _blank para una nueva pestaña).
    Ejemplo 1: <a href="https://www.ejemplo.com" target="_blank">Visita Ejemplo</a>
    Ejemplo 2: <a href="mailto:correo@ejemplo.com">Envíanos un correo</a>
    Ejemplo 3: <a href="tel:+1234567890">Llámanos</a>

2. <em> (emphasis, énfasis): Indica énfasis en el texto, generalmente mostrado en cursiva.
    Ejemplo: <em>Este texto está enfatizado.</em>

3. <strong> (strong, fuerte): Indica fuerte importancia en el texto, generalmente mostrado en negrita.
    Ejemplo: <strong>Este texto es importante.</strong>

4. <small> (small, pequeño): Representa texto en letra pequeña, como notas al pie o avisos legales.
    Ejemplo: <small>Este es un texto pequeño.</small>

5. <big> (big, grande): Representa texto en letra grande, aunque, esta etiqueta esta obsoleta y se 
recomienda usar css.
    Ejemplo: <big>Este es un texto grande.</big>

6. <code> (code, código): Muestra fragmentos de código de computadora en un estilo de fuente 
monoespaciada.
    Ejemplo: <code>int x = 10;</code>

7. <sub> (subscript, subíndice): Muestra texto como subíndice.
    Ejemplo: H<sub>2</sub>O

8. <sup> (superscript, superíndice): Muestra texto como superíndice.
    Ejemplo: <sup>2</sup>

9. <i> (italic, cursiva): Muestra texto en cursiva, pero sin indicar énfasis semántico (usar 
<em> para énfasis).
    Ejemplo: <i>Texto en cursiva.</i>

10. <b> (Bold, negrita): Muestra texto en negrita, pero sin indicar fuerte importancia semántica
(usar <strong> para importancia).
    Ejemplo: <b>Texto en negrita.</b>

11. <u> (underline, subrayado): Muestra texto subrayado.
    Ejemplo: <u>Texto subrayado.</u>

12. <cite> (citation, citación): Define el título de un trabajo (por ejemplo, un libro, una canción,
una película, etc.). Suele mostrar el texto en cursiva.
    Ejemplo: <cite>El Quijote</cite>

13. <wbr> (word break, salto de palabra): Indica dónde se puede romper una palabra si es necesario
para ajustar el texto.
    Ejemplo: TextoLargo<wbr>ConPosibleSalto

14. <span> (span, abarcar): Contenedor genérico en línea que se utiliza para aplicar estilos o
manipular partes específicas del texto con CSS o JavaScript. Si se utiliza sola no produce efectos
visibles.
    Ejemplo: <span style="color: red;">Texto rojo</span>

15: <br> (break, salto): Inserta un salto de línea.
    Ejemplo: Línea 1<br>Línea 2

    - Besitos, Sofi♥.